<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ 'theme.css' | asset_url }}">
    <title>Seiko Watch</title>
</head>
<body>
    <header>
        <div class="logo">
            <a href="{{ '/' }}">
                <img src="{{ 'logo.png' | asset_url }}" alt="Seiko Watch Logo">
            </a>
        </div>
        <nav>
            <ul>
                <li><a href="{{ collections.all.url }}">Shop</a></li>
                <li><a href="{{ pages.about.url }}">About</a></li>
                <li><a href="{{ pages.contact.url }}">Contact</a></li>
            </ul>
        </nav>
    </header>
</body>
</html>